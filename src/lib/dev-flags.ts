// TEMP TEST FLAG — set back to false to restore normal fetching.
// When true, the records fetch and the MinerWars cycle-tracker fetch are forced
// to fail so the fetch-failed modal can be exercised end-to-end.
export const FORCE_FETCH_FAIL = false;
